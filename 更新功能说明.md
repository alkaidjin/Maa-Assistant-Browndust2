# BD2MAA 更新助手 / 启动器 使用说明

> 适用项目：`Maa-Assistant-Browndust2`（基于 MaaFramework + MXU）
> 新增/改动文件：`BD2MAA-Updater.ps1`、`launcher.bat`、`updater_config.json`
> 实现方式：**纯 PowerShell，零依赖，无需 Python**

> 📌 **三份文档的分工**：本文是**操作手册**（启动器 / 自动更新 / 命令行参数 / 配置项 / 日志清理 / 故障排查）；
> 项目简介、功能概览与下载方式见 [README](README.md)；第三方许可与来源链条见 [NOTICE](NOTICE.md)。

本功能在**软件开启时自动检测 GitHub 仓库新版本**：
- 发现新版本 → 弹出**选择对话框**（含版本号与更新日志），由用户决定；
- 用户选「一键更新」→ **前台显示下载进度** → 下载 → **自动覆盖旧版本**；
- 用户选「暂不更新」→ **直接打开 `mxu.exe`** 进入软件。

---

## 一、怎么用（最重要）

> ⚠️ 以后请通过 **`launcher.bat`** 来打开软件，**不要**再直接双击 `mxu.exe`。
> 这样每次启动都会先检测一次更新。**首次启动时 launcher 会自动在根目录生成带图标的 `MaaBd2.lnk`**（指向 launcher.bat 与本项目的 `mxu.ico`），之后你可以双击它，或把它拖到桌面。

### 快捷方式与程序图标（推荐）
根目录的 `MaaBd2.lnk` 是**启动时自动生成**的（不是打包好的，避免不同用户机器路径不同导致 lnk 失效）：
- **首次启动**：launcher.bat 会自动创建 `MaaBd2.lnk`，目标 = `launcher.bat`，工作目录 = 项目根目录，图标 = `mxu.ico`
- **之后每次启动都会重建**（幂等、不耗资源），保证永远指向当前路径
- 想放到桌面：把 `MaaBd2.lnk` 拖到桌面即可；或右键 `launcher.bat` → 发送到桌面快捷方式，再手动改图标

#### 程序图标（`mxu.exe` / 启动器）
| 文件 | 作用 |
|------|------|
| `mxu.ico` | 图标源文件（多尺寸 16/24/32/48/64/128/256，已内嵌圆角） |
| `tools\rcedit-x64.exe` | 真正执行 PE 资源写入的工具。启动器内置的 `Apply-ExeIcon` 按「`mxu.exe` 大小 + 时间戳」指纹判断是否需要重打，指纹记在 `cache\icon_stamp.txt`；`mxu.exe` 正在运行时自动跳过 |
| `tools\apply_icon.ps1` | 等价的独立脚本（写图标 + 重建 `MaaBd2.lnk`，并首次备份 `mxu.exe` 到 `cache\mxu.exe.bak`）。**启动器不调用它**，需要手动重打图标时用 `powershell -File tools\apply_icon.ps1` |

> MXU 自更新会替换 `mxu.exe`，图标可能变回默认。**`launcher.bat` 每次启动时会自动检测并按需补图标 + 重建 `MaaBd2.lnk`**，无需手动操作。

---

## 二、检测到新版本时的弹窗

| 按钮 | 作用 |
|------|------|
| **⬇ 一键更新** | 前台弹出下载进度窗口（带进度条与 MB/百分比），下载完成后**自动解压并覆盖旧版本**，然后启动软件 |
| **暂不更新** | 关闭弹窗，**直接启动 `mxu.exe`** 进入软件（下次启动仍会检测） |

> 说明：选择「一键更新」后，助手会**自动下载并覆盖**旧版本（无需你手动解压）。
> 更新时**会保留你 `config/` 目录下的已有配置**，仅补齐缺失的默认配置项，详见第五节。

### 20 秒无操作自动跳过（v26.09.7 新增）

弹窗内含倒计时提示「无操作 X 秒后自动跳过更新」：

- **20 秒内**点「⬇ 一键更新」或「暂不更新」→ 正常生效（计时器立即停）
- **20 秒内无任何点击** → 弹窗自动关闭、视作「暂不更新」、直接启动 `mxu.exe` 进入软件
- 设计动机：用户**无人值守**场景（挂机 / 远程启动 / 离开座位）不应被卡在更新窗口；定时自动跳过才能让「自动化日常」真正无人值守
- 倒计时只在发现新版本时出现；已是最新版本直接进软件，无弹窗无倒计时

---

## 三、更新失败时的手动兜底

正常流程下「一键更新」会自动完成覆盖，无需手动操作。若下载/解压异常，助手会提示你前往发布页手动下载：

1. 打开 `updates/` 文件夹，得到如 `MABd2vXX.XX.X.zip` 的压缩包（若已在下载目录）。
2. （可选）**备份你的安装目录下的 `config\mxu-MaaBrownDust2.json`**——这是你的任务实例与设置。注意：本发布 zip **根本不打包**这个文件，所以即使不备份也不会被覆盖（MXU 首次启动会自动生成默认实例；老用户已有的实例100% 保留）。
3. 把压缩包解压到**你的软件根目录**（即 `launcher.bat` 所在目录）并覆盖（推荐用 7-Zip / Bandizip 等“合并覆盖”模式；解压后根目录没有 `MaaBd2.lnk`，首次启动 launcher.bat 会自动重建）。
4. 重新通过 `launcher.bat` 进入软件。

---

## 四、命令行参数（高级）

在 `launcher.bat` 后面加 PowerShell 开关即可，例如 `launcher.bat -Demo`：

| 参数 | 作用 |
|------|------|
| `-Force` | 忽略缓存时间，强制重新请求 GitHub 检测 |
| `-Demo` | **演示弹窗**：即使本地已是最新也弹出更新窗口，用于预览效果 |
| `-Test` | 仅做逻辑自检（读取当前版本、拉取最新 release、选资源、比较版本），不弹窗、不启动软件 |
| `-Repair` | **修复安装**（v26.09.11 新增）：不管版本是否相同，重新下载最新包并**整包覆盖**一遍。用于「上次更新只装了一半 / 感觉文件没更新 / 杀软拦截过」的情况；`config/` 用户配置同样不会被覆盖 |
| `-Force` / `-Demo` / `-Test` / `-Repair` | 这四个开关都会**忽略 6 小时缓存、强制联网检测**（普通启动在 6 小时内只检测一次，可能静默进游戏） |

> 提示：本地版本看 `interface.json` 的 `version` 字段（随发布包更新，不需要手动改）。
> 更新判据是「线上 tag 比本地新」——本地版本 ≥ GitHub 最新 tag 时不会弹窗。
> 想先看弹窗效果请用 `-Demo`，它无视版本比较、强制弹出。
>
> **更新过程有三道自检（v26.09.11 起），任何一道不过都不会显示「更新完成」**：
>
> | 时机 | 检查 | 不过时怎么办 |
> |------|------|--------------|
> | 装前 | 解压出来的包内 `interface.json` 版本号必须 == 该 release 的 tag，且必须含 `mxu.exe` | 取消覆盖（你的目录一个字不动），提示稍后重试 / 手动下载。主要拦「该 release 的压缩包刚被替换或还在上传」 |
> | 装中 | 逐文件重试 3 次；一个文件失败**不再中断整次更新**，其余文件照常覆盖 | 结束时报出**具体是哪些文件**没覆盖（多半被杀软/占用） |
> | 装后 | 复核磁盘上 `interface.json` 的版本号 == tag，且 `mxu.exe` / `launcher.bat` 等关键文件在位 | 报「更新未完成」并给出修复步骤（关杀软 → `launcher.bat -Repair`） |
>
> 另外，**更新前若检测到 `mxu.exe` / `go-service.exe` 仍在运行**，会先弹窗询问是否自动关闭
> —— 程序文件被占用时覆盖会失败，那正是「更新完版本号却没变 / 只装了一半」最常见的成因。

---

## 五、可配置项（`updater_config.json`）

| 字段 | 默认 | 说明 |
|------|------|------|
| `repo` | `alkaidjin/Maa-Assistant-Browndust2` | 检测的 GitHub 仓库 |
| `check_interval_hours` | `6` | 两次联网检测的间隔（避免触发 GitHub 60次/小时限流） |
| `asset_keywords_include` | `["MABd2","win","BD2","Browndust"]`（随包配置里精简为 `["MABd2"]`） | 优先匹配的压缩包关键字 |
| `asset_keywords_exclude` | 含 `source code`/`src`/`debug`/`linux`/`macos`/`darwin` 等 | 排除的资源（源码包、调试包、其他平台） |
| `protected_dirs` | `["config"]` | **更新时这些目录的"已有文件"不覆盖**（仅新增缺失的默认配置）。`config/mxu-MaaBrownDust2.json` 本身就不在 zip 里，参见下方"config/ 保留策略"小节 |
| `download_dir` | `updates` | 下载保存目录（相对项目根） |
| `log_retention_days` | `7` | `debug/` 下日志与调试截图的保留天数，超过即自动清除（见第八节） |
| `log_compact` | `true` | 启动时自动把 `debug/maa.log` / `maa.bak.log` 精简成可读的任务时间线（见第八节） |
| `log_compact_keep_recognition` | `false` | 精简时是否保留每一次识别的结果行。调识别参数时设 `true`，平时保持 `false` |
| `open_folder_after` | `false` | 更新完成 / 启动完成后是否自动打开软件根目录窗口 |
| `mirror_url_prefixes` | `["https://ghfast.top","https://gh-proxy.com"]` | 下载镜像重试链，详见第九节 |

> `updater_config.json` 里**没写**的键会回落到 `BD2MAA-Updater.ps1` 顶部 `$DEFAULTS` 的默认值（例如 `protected_dirs` 默认 `["config"]`），所以配置文件可以只保留你想改的项。
> 早期版本（≤v26.09.6）的 `ensure_window_ready` 开关已随第十节的补丁一并移除；配置里若还留着这一行，会被直接忽略。

缓存文件 `updater_cache.json` 由程序自动维护，记录上次检测时间，无需手动改。

### 关于 `config/` 的保留策略（用户最关心）
更新覆盖时，`BD2MAA-Updater.ps1` 采用**双层保护**：

**第一层 · 发布 zip 根本不打包用户配置**
- `config/mxu-MaaBrownDust2.json`（每个用户的实例 + 任务 ID + enabled 状态 + optionValues）**不在 zip 里**——它本质是用户运行时产物，应该跟着机器走、不跟着版本走。
- 这意味着无论是**通过 launcher 走一键更新**、还是**手动解压覆盖**到你的软件根目录，这个文件都**永远不会被新版本覆盖**。
- 新用户首次启动 MXU 时，MXU 会自动按内置默认值生成一份 `mxu-MaaBrownDust2.json`，里面包含一份可用的默认任务实例。

**第二层 · 启动器的 `Copy-Update` 也会兜底**
- 对于 zip 里**确实存在**的 `config/` 文件（比如未来的某个新选项），`BD2MAA-Updater.ps1` 的 `Copy-Update` 函数带 `protected_dirs = ['config']`——已存在的文件保留，仅补齐缺失的默认项。

> 已用自动化测试验证：模拟更新中，既有用户配置 `config/user_existing.json` 保持不变，
> 而新默认项 `config/default_new.json` 被成功补齐，根目录 `mxu.exe`/`interface.json` 被更新。

### 关于覆盖的"原子性"与中途异常
- **大文件（≥ 1 MB，如 `mxu.exe`、模型 `.onnx`）** 走"先写 `<file>.new`、再 `Move-Item -Force` 改名覆盖"的两段式原子覆盖。
  中途断电 / 磁盘满 / 进程被杀时，旧文件不会变成半截，新文件留在 `*.new` 等下次启动清理。
- **小文件（json / png / txt）** 走直接 `Copy-Item -Force`——损坏也只是文本/配置损坏，重启会再被新版本覆盖回来。
- **下载失败**也会自动清理掉残留的半截 zip，`updates/` 不会越用越胖。

> 📌 **关于 `resource/`**：它存放的是软件自带的**任务流水线（`pipeline/`）、OCR 模型（`model/`）、识别图片（`image/`）与游戏数据（`gamedata/`）**，属于**软件资源而非用户配置**。每次更新都会**随新版本一并覆盖更新**——这正是预期行为：旧的任务定义 / 模型若不被更新，反而可能导致识别或执行出错。它不会被“保留”，也无需保留；真正需要保留的只有 `config/` 下的用户任务实例。

---

## 六、MXU 内置更新入口（已接入 Mirror酱）

MXU 的「设置 → 更新」分区（含「检查更新」按钮与 Mirror酱 CDK 输入框）**只在项目声明了 `mirrorchyan_rid`
（Mirror酱资源 ID）时才渲染**。本项目**已接入 Mirror酱**，`interface.json` 中声明的资源 ID 为：

```json
"mirrorchyan_rid": "Maa-Assistant-Browndust2"
```

- **有 CDK**：在 Mirror酱 官网为本项目（资源 ID `Maa-Assistant-Browndust2`）购买 CDK，填入 MXU「设置 → 更新」，即可使用高速下载与自动更新；
- **无 CDK**：完全不影响使用 —— 版本检测与更新照旧由外置启动器负责：`launcher.bat` → `BD2MAA-Updater.ps1` → 检测 GitHub Releases → 弹窗 → 一键下载并覆盖（保留 `config/`）。两条渠道互不冲突；
- 新版本由仓库内置的 GitHub Actions（`mirrorchyan_release` 工作流）在 Release 发布时**自动上传**到 Mirror酱，无需人工搬运。

`interface.json` 中的 GitHub 地址与版本号仍然保留：

```json
"github": "https://github.com/alkaidjin/Maa-Assistant-Browndust2",
"version": "v26.09.11"
```

> `version` 由打包流程在发包时自动写入（当前值随版本变化），不需要手动维护。

> ⚠️ 购买 CDK 请认准资源 ID `Maa-Assistant-Browndust2` —— 与它名称相似的那个第三方项目 `MaaBD2`（资源 ID `MFABD2`）**不是本项目**，辨认要点见 [README](README.md) 的「关于 Mirror酱」一节。

---

## 七、常见问题

- **双击 .bat 一闪而过 / 没反应？** `launcher.bat` 现以纯 PowerShell 运行（无需 Python）。
  若仍异常，请右键 .bat「以管理员身份运行」或手动执行
  `powershell -NoProfile -ExecutionPolicy Bypass -File "BD2MAA-Updater.ps1"` 查看报错。
- **检测一直失败 / 没弹窗？** 检查网络是否能访问 `api.github.com`；GitHub 未登录限速 60 次/小时，
  频繁测试可用 `-Force` 但请节制。
- **我想恢复成直接打开 mxu.exe？** 直接双击 `mxu.exe` 即可，更新检测功能只是“额外的一层”，不影响软件本体。
- **更新会弄丢我的任务配置吗？** 不会。`config/mxu-MaaBrownDust2.json` **根本不在 zip 里**——新用户首次启动 MXU 自动生成默认实例，老用户的真实实例 100% 保留。即使你手动把 zip 解压覆盖到软件根目录也一样安全（详见第五节）。
- **根目录偶尔出现一个 0 字节的乱码文件（形如 `鍚?姩`）？** 它的来源是**旧版 `launcher.bat`**：注释里
  `-> 启动 mxu.exe` 的 `>` 被 cmd 当成重定向符，于是每次启动都会造出一个 0 字节文件（文件名是 UTF-8「启动」
  经 GBK 代码页显示的结果）。**v26.09.7 已把 `launcher.bat` 改写为纯 ASCII 并清掉 `>`，不再产生该文件**；
  早期曾用「守护进程清理 / 隐藏」的方式兜底，v26.09.7 修订版已把该装置**整体删除**（源头没了，装置只会自我抵消）。
  `.gitignore` 仍忽略该文件名，即使老目录里有残留也不会进入 git 仓库。无需干预。
  > 顺带一条通用经验：`.bat` 请保持**纯 ASCII + CRLF 行尾**，注释里也不要写 `>`。cmd 是逐字节解析的，
  > 含非 ASCII 字节会被按 GBK 误解码、切错命令边界，轻则刷「不是内部或外部命令，也不是可运行的程序或
  > 批处理文件」，重则整行边界被打乱、脚本被截断——这正是 v26.09.7 修掉的启动 bug。
- **点「启动任务」后 MXU 报「未搜索到任何窗口」秒退？** 该问题根因在 **MXU 上游**（等待窗口就绪的循环被
  `savedDevice.windowName` 门槛短路），v26.09.7 随包把 MXU 升到 v2.5.3 后**已由上游修复**，启动器不再注入
  任何字段。若仍遇到，在 MXU「连接设置」里手动选一次游戏窗口即可恢复（详见第十一节第 2 条）。
- **更新完发现旧的说明文档 / 教学视频不见了？** 那是**有意为之**：改名后的老文档、老视频会被启动器
  按 `retired_files.json` 自动清掉，只留当前版本那一份（详见第十二节）。清单里没有的文件（包括你自己
  放进去的任何东西）不会被删。

---

## 八、`debug/` 目录的自动精简与清理

`debug/` 是 MXU 与 MaaFramework 共同写日志的地方，长期运行会无限增长。它包含三类产物：

| 类型 | 示例 | 说明 |
|------|------|------|
| 日志 | `mxu-web-YYYY-MM-DD.log`、`mxu-agent.log`、`maa.log`、`go-service.log`、`mxu-tauri.log` | MXU / MaaFW 运行时输出 |
| 调试截图 | `StartGame_2026.09.05-09.34.43.115.png` 等 | `maa_option.json` 里 `save_on_error: true` 时，识别失败自动留存的现场截图 |
| 归档 | `archive/maa.log.20260911-192615.log.gz` | 精简前的原始日志（gzip，约为原始体积的 4%） |

### 8.1 第一步：精简 `maa.log`（`tools/compact_log.ps1`）

**为什么需要**：MaaFramework 的**文件**日志固定以 Trace 级别全量落盘，`config/maa_option.json` 里的
`stdout_level` **只管控制台，管不到文件**（这是上游的设计，没有配置项可以降低）。实测跑一轮日常任务就会产生
**9~16 MB / 2~5 万行**，其中约 99% 是事件通知、识别过程、主进程与 Agent 进程的重复投递。

**做了什么**：把"消防水管"式的原始日志压成一份能从头读到尾的「任务时间线」：

| 处理 | 内容 |
|------|------|
| 保留 | `WRN` / `ERR` / `FATAL`；任务边界；节点边界；`Task timeout` / `invalid node id`；`save on error` 路径；会话起止 |
| 丢弃 | 全部 `TRC` / `DBG` 行；`AgentClient::ctx_event_sink` 事件回显；`NextList.*` / `Recognition.*` / `Action.*` 事件 |
| 折叠 | 连续重复的同类警告（`Wrong ocr_result size` 一次能刷几百行 → 1 行 + `同类警告共 N 条，已折叠`） |
| 去重 | 同一事件被主进程 / Agent 进程重复投递的记录（时间差 ≤ 20ms） |
| 改写 | 事件行 → `<时间>  任务/节点  开始/完成/失败  <名字>` 单行形式 |

**实测效果**（一轮 12 任务的日常）：

```
原始    :  8,990,366 字节 / 27,905 行
精简后  :     52,396 字节 /    657 行
下降    : 行数 97.7%   体积 99.4%
```

精简后的事件时间线长这样，失败点一眼可见：

```
2026-09-11 18:14:41.261  任务    开始  StartPVP
2026-09-11 18:15:21.850    节点  开始  PVPBegin
2026-09-11 18:15:31.890    节点  开始  WaitForPVP
2026-09-11 18:16:02.655    节点  开始  Quit
2026-09-11 18:16:22.919  [WRN] Task timeout [pretask.name=Quit] [duration_since(start_clock)=20263ms]
2026-09-11 18:16:22.919    节点  失败 <<<  Quit
2026-09-11 18:16:22.952  [INF] save on error to F:/BD2MAA/debug/on_error/Quit_2026.09.11-18.16.22.920.png
2026-09-11 18:16:22.952  任务    失败 <<<  StartPVP
```

**安全设计**：

- 改写前先把原始日志 gzip 归档到 `debug/archive/`，**不会丢任何信息**（实测 8.9 MB → 385 KB，约 4%）；
- **按文件粒度**检测占用：MXU 正在写入的 `maa.log` 会被自动跳过，轮转出来的 `maa.bak.log` 照常精简。
  比"看进程在不在"更准，也因此即使开着 MXU 重新启动启动器也不会破坏文件；
- 完全幂等：对已精简的文件再跑一遍会直接报「无需处理」，不会重复归档。

### 8.2 第二步：按保留天数清理

**保留策略**：只保留最近 `log_retention_days` 天（默认 **7 天**），更早的一律删除（含 `archive/` 里的归档）。

启动器每次启动时都会自动执行「精简 + 清理」（见 `BD2MAA-Updater.ps1` 的 `Invoke-LogCleanup`）。
想手动执行或预览：

```bat
:: 先看能压掉多少，不动文件
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\compact_log.ps1" -DryRun

:: 真跑一次精简
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\compact_log.ps1"

:: 正在调识别参数：连每次识别的结果行一起保留
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\compact_log.ps1" -IncludeRecognition

:: 只清理过期文件（不做精简）
powershell -NoProfile -ExecutionPolicy Bypass -File "tools\clean_logs.ps1" -Days 7
```

> 只想改策略、不想敲命令行？直接改 `updater_config.json` 里的 `log_retention_days` / `log_compact` 即可，启动器会读它。
> 以上操作**只作用于 `debug/`**，不会碰 `config/`、`resource/`、`tasks/` 或任何其他目录。

---

## 九、加速下载：自动镜像源链（v26.09.4 新增）

### 为什么需要

GitHub Releases 在国内普遍被严重限速（实测最低 10~30 KB/s，下一个 60 MB 的 zip 可能要 30 分钟以上）。
更换下载器（`BITS` / `aria2` / `curl`）都无效——**限速在源站，不在客户端**。

### 解决方案：updater 内置「镜像前缀重试链」

启动器 `BD2MAA-Updater.ps1` 现在的下载流程：

```
1. 取配置的 mirror_url_prefixes 列表（当前默认 2 个国内常见 GitHub 镜像）
2. 把每条镜像前缀拼到 GitHub 原 URL 前面，得到一组候选 URL
3. 顺序下载，第一个通过完整性校验的就用，剩下的跳过
4. 全部失败 → 最后回退到 GitHub 原 URL（终极兜底）
```

下载窗会显示当前正在用的源（例如 `源：ghfast.top`），方便你看哪个镜像在工作。

> 镜像并不是都可靠：部分镜像对不存在的直链会返回 **HTTP 206 + 一个几 KB 的 HTML 错误页**，
> 下载器不报错，直到解压才炸「找不到中央目录结尾记录」。所以每下载完一个候选源都会做**三层校验**
> （字节数 == 官方 asset 大小 → `PK` 魔数 → zip 中央目录可读），不合格就删掉残留、自动切下一个源。

### 默认镜像列表（`$DEFAULTS` / `updater_config.json`）

```json
"mirror_url_prefixes": [
    "https://ghfast.top",
    "https://gh-proxy.com"
]
```

> 这份列表本身不带 `https://`，会自动补；用户也可以直接写 host（例如 `cdn.your-corp.com`，会自动补 `https://`）。
> 2026-09-11 实测后，`ghps.cc`（返回假 zip）与 `mirror.ghproxy.com`（连接超时）已从默认列表移除——原因也记录在 `updater_config.json` 的 `_doc_mirror_history` 字段里。

### 配置项：`updater_config.json → mirror_url_prefixes`

| 动作 | 怎么改 |
|---|---|
| **关闭镜像，只用官方** | `"mirror_url_prefixes": []`（实测海外/海外代理场景可加速首次检测） |
| **追加公司/家用代理** | 在数组里加自己的条目，例如 `"https://your-proxy.example.com/gh"` |
| **替换默认的 2 个** | 直接整个覆盖数组。格式：每条是「主机名」或「完整 URL 前缀」 |
| **优先级** | 数组里**靠前**的先试。如果常用的 `ghfast.top` 不稳，把它删掉或移到末尾 |

### 怎么找到你环境能用的镜像

**没时间试错？最快路径**：

1. 浏览器直接打开列表里的 host（例如 `https://ghfast.top/`），哪个能进就是你能用的；
2. 把不能访问的删掉，能访问的提到前面；
3. 重启 `launcher.bat`，下次更新就能从你配置的镜像下。

**自带工具**：v26.09.4 起，下载窗会在「源」一行显示当前用的镜像名。下载卡住时可以按 `Ctrl+C` 关掉窗口，updater 自动切下一个候选源；全部失败的话会弹窗让你去 release 页手动下载。

### 常见问题

- **「源」显示 `ghfast.top` 但下载还是慢？** 该镜像当前可能临时限速。把它挪到数组末尾，下次先试别的。如果全部都慢，先确认你的本地网络到这些镜像的连通性（不是 GitHub 的连通性）。
- **下载完解压报「找不到中央目录结尾记录」？** 那是下到了镜像返回的 HTML 错误页（详见上文「三层校验」）。当前版本会自动识别并换源；如果仍报错，把该镜像从列表里删掉即可。
- **公司代理怎么配？** 比如公司搭了 `http://proxy.local:7890` 的代理并对外暴露 GitHub 镜像。把它写成 `"http://proxy.local:7890/https://github.com"` 这种形式加到数组最前面。
- **关闭镜像反而比开镜像快？** 那大概率你走海外网络。在配置里写 `"mirror_url_prefixes": []` 即可只走官方。

---

## 十、历史：MXU「未搜索到任何窗口」秒退及其过渡补丁（v26.09.6，已于 v26.09.7 废弃）

**现象**：在 MXU 里点「启动任务」，前置程序（或你手动）已拉起游戏 exe，MXU 却紧接着报 `未搜索到任何窗口`，任务被秒退。

**根因**（不是检测太快）：MXU 内部**确实有**一个 300 次 × 1 秒（共 5 分钟）的「等待窗口就绪」循环，但它被一条守卫卡死 —— 新用户从来没在 MXU「连接设置」里手动选过窗口，`savedDevice` 字段根本不在 config 里，于是守卫短路、落到「一次性搜索」分支，这一次搜不到就直接 return false。

**v26.09.6 的临时补丁**：针对该守卫，启动器在拉起 `mxu.exe` 前往 `config/` 里注入 `savedDevice.windowName = "BrownDust II"`，并把游戏 `preAction` 的 `waitForExit` 翻成 `false` 以绕过守卫；另配 `updater_config.json → ensure_window_ready` 总开关，以及 `tools/启动游戏并等待窗口.bat` + `tools/wait_game_window.ps1` 手动兜底工具。

**v26.09.7 起已全部移除**：MXU v2.5.3 从上游把守卫改为 `shouldWaitAfterPreActions = !!controller`（只看 controller 是否存在），补丁失去意义 —— 包括 `Ensure-WindowReady` 注入函数、`ensure_window_ready` 开关、`tools/wait_game_window.ps1`、`tools/启动游戏并等待窗口.bat`。配置里若还留着 `ensure_window_ready`，删不删都行，它已不起任何作用。

> 本节作为 v1.13.1 → v2.5.3 之间的兼容补丁历史保留，便于老用户回看；具体的升级内容见第十一节。

---

## 十一、底层升级：MXU v1.13.1 → v2.5.3 + MaaFramework v5.7 → v5.13（v26.09.7 新增）

> 自 v26.09.7 起，随包发布的 `mxu.exe` 锁定 **v2.5.3**，`maafw/*` 锁定 **v5.13.0**；两者的上游 tag 记录在 `version.json`。
> 升级前的 `maafw/`（21 个条目）、`mxu.exe`、`config/mxu-MaaBrownDust2.json`、`interface.json` 与 `version.json` 已备份到作者的 `cache/backup_v26.09.6/`。注意 **`cache/` 不随发布包派发**，只存在于作者机器上 —— 你自己升级前也建议照此备份一份，任何时候把备份拷回原位置即可回滚。

### 解决了哪些问题

1. **任务启动失败：`Tasker not properly initialized`** —— v1.13.1 的 `tasker handle` 复用死状态，手动停止一次任务后下次「开始任务」立刻挂；v2.5.3 在 `if !tasker.inited()` 时自动丢弃并重建。
2. **`savedDevice.windowName` 强制门槛** —— 新用户首次启动时等待循环被守卫短路 → `未搜索到任何窗口` 秒退；v2.5.3 守卫改为 `shouldWaitAfterPreActions = !!controller`（详见第十节）。
3. **`preAction.args` 引号解析** —— v1.13.1 的 `split_whitespace()` 不识别引号，路径含空格必爆；v2.5.3 改用 `shell_words::split`，原生支持 `"C:\Program Files\..."` 这类带空格参数。
4. **`useCmd` 新增** —— 可显式选择 `cmd /c` 走 .bat，避免把 .bat 误当 PowerShell 调用。
5. **窗口稳定延迟 `preActionConnectDelaySec`（默认 5 秒）** —— v1.13.1 找到窗口后立刻连接，游戏还在加载动画 → 偶发连接错误；v2.5.3 新增沉降延迟。
6. **minimaafw 检查** —— v2.5.3 启动时校验 `MIN_MAAFW_VERSION = "5.12.1"`，旧版 maafw 启动即报错，防止误用严重不兼容的运行时。

### 升级后回退了哪些之前打的补丁

| 补丁文件 | 移除原因 | v2.5.3 替代机制 |
|---|---|---|
| `BD2MAA-Updater.ps1` → `Ensure-WindowReady` 函数 | 不再需要注入 `savedDevice` | `shouldWaitAfterPreActions = !!controller` |
| `tools/wait_game_window.ps1`、`tools/启动游戏并等待窗口.bat` | 等待循环已内置到 MXU | 原生 300×1s 等待 + 5s 沉降延迟 |
| `updater_config.json` → `ensure_window_ready: true` | 总开关失去对象（注入函数已删） | — |
| `更新功能说明.md` 第十节 | 升级后默认行为已符合预期 | 该节保留作为历史记账 |

### 升级 / 回滚步骤（参考）

```text
1. 备份：把整个 maafw/ + mxu.exe + config/ 下实例配置 备份到 cache/backup_v<old-ver>/
2. 下载 MXU-<platform>-<arch>-v2.5.3.zip + MAA-<platform>-<arch>-v5.13.0.zip
3. 取 MXU zip 里的 mxu.exe 单文件；取 MaaFramework zip 里 bin/* + share/MaaAgentBinary/*
4. 覆盖：mxu.exe 替换；maafw/* 全套替换；resource/model/ocr/*.onnx 保持不动
5. 首次启动时 MXU 自动迁移 config schema（如新增 preActionConnectDelaySec 默认 5 秒）
6. 跑一次 launcher.bat，看 mxu-web 日志确认无「Tasker not properly initialized」、「未搜索到任何窗口」
7. （可选）回滚：把 backup 目录拷回即可（不需要重装）
```

### 版本指纹核对

`maafw/MaaFramework.dll` 的 sha256 前 16 位：v5.13.0 为 `d4503d525561a7be`，v5.7.0-alpha.2 为 `d256e43121ad69a1`。

⚠️ **核对升级必须看 sha256，不要看文件大小** —— v5.13.0 的多数 DLL 反而比 v5.7 更小（上游重构所致），且 `MaaRecordControlUnit.dll`、`MaaReplayControlUnit.dll` 大小前后一致但内容已经变了。

### 第三方致谢

本次升级后 BD2MAA 链接了 LGPL-3.0 的 MaaFramework，因此仓库根新增了独立的 [NOTICE.md](NOTICE.md) —— MaaFramework / MXU / MaaEnd / essinn-1 / PaddleOCR / rcedit 六项的履约与来源说明全部记在那里，本节不再重复。

### 仍未解决 / 已知遗留

- `interface.json` 保留 PlayCover 入口，但所有 task 限定 Win32 → macOS 用户会困惑（`interface_version: 2` 的上游行为，暂不动）
- pipeline 长链缺统一容错（OCR 失败靠 fallthrough 到 StartGame）—— 与底层版本**无关**，一直在 backlog 上
- 「乱码标记守护」（`BD2MAA-Updater.ps1` 的 `Protect-Marker` + `tools/hide_marker.ps1`）**已整体删除**：它是「标记由 MXU 写出」这一**误判**的产物，成因已查明确为旧版 `launcher.bat` 注释里的 `>` 重定向（见第七节 FAQ）
- `tools/apply_icon.ps1` 目前没有调用方（图标实际由启动器的 `Apply-ExeIcon` + `rcedit-x64.exe` 完成），保留作为手动修复工具
- 本次升级**没动**的东西：`interface.json` schema、`tasks/*.json` 与 `resource/pipeline/*.json`（全部 drop-in 兼容、零 custom_xxx 节点）、`resource/model/ocr/*.onnx`、`agent/go-service` 配置，以及你的 `config/mxu-MaaBrownDust2.json` 实例

---

## 十二、旧版遗留的说明文档 / 教学视频 / 旧任务文件会被自动清理（v26.09.11 新增）

### 问题：一键更新只「覆盖 + 新增」，从不删除

更新逻辑是**把新包里的文件覆盖过去**（大文件走「先写 `.new` 再改名」的原子替换），
**包里没有的文件不会被动**。这在正常升级时没问题，但**文件一旦改名**，老的那份就会一直留着：

| 版本 | 当时的说明文档 / 视频 |
|------|----------------------|
| ≤ v26.09.6 | `注意事项1-----使用前必看！！！.txt` + 三张 `注意事项N——…png` + `github地址.txt` |
| v26.09.7 ~ v26.09.10 | `重要！注意事项！！使用前必看！！！.pdf` + `重要教学！！…教程 .mp4` |
| v26.09.11 起 | `#请务必打开此文档查阅#含使用方式以及常见问题解答.docx` + `#使用本软件自动打开游戏并设定游戏分辨率的教学视频# .mp4` |

于是升到 v26.09.11 的用户目录里可能同时躺着 **三份同内容的注意事项文档 + 两个教学视频**：
既白占十几 MB，又让人不知道该点哪一份（**这份文档每次都是更好更新的那一版**）。

### 解决：随包派发的清理清单 `retired_files.json`

- 包内新增 `retired_files.json`：列出「以前发给过用户、现在不再派发」的文件（相对软件根目录）。
- 启动器 `BD2MAA-Updater.ps1` 会在**每次启动** + **每次自动更新完成后**按它清理一遍（幂等）。
  更新完成那一刻读到的就是**新版清单**（该文件刚被覆盖），所以改名当次就能生效。
- 安全边界（任一不满足就跳过该项）：
  - 只删清单里的**显式相对路径**，含 `..` 或绝对路径一律跳过；
  - 跳过受保护目录（`config/`，见 `updater_config.json → protected_dirs`）；
  - 清单缺失 / 解析失败 → 静默跳过，**绝不阻断启动**；
  - 清单里没有的文件一律不碰 —— 你自己放进去的文档、脚本、截图都不会被删。
- ⚠️ **首次升级到 v26.09.11 时，跑的还是旧启动器**（新代码要等文件覆盖完才存在），
  所以那批老文件会在**下一次启动软件时**被清掉。想立刻清理就再启动一次 `launcher.bat`，
  或者手动删掉上表里旧版本那几份（删除是纯收益：当前版本的那一份才是最新的）。

### 给维护者：以后怎么加

改随包文件的名字 / 下架某个随包文件时：**只需把旧名字加进 `retired_files.json`**，三件事自动成立 ——

1. 启动器下次启动就把它从所有用户目录里删掉；
2. 打包器 `EXCLUDE_FILES |= RETIRED_FILES`（同一份清单）保证它**永远不会再被打进包**；
3. 打包前的自检会拦住写错的情况：`[R1]` 磁盘上不能还存在该文件、`[R2]` 该路径必须在 git 历史里
   （挡拼写错误）、`[R3]` 若仓库仍跟踪着它会给出提醒；产出 zip 还会复核 `[V9] 退休清单撞车 = 无`。

> 若某份老文件你想留着（做纪念 / 存档），先复制到软件目录之外 —— 清单只删软件根目录里的那几份。

### 例外：会被脚本当「管线」加载的文件（v26.09.11 起）

`resource/pipeline/*.json` 这类**会被 MaaFramework 当管线加载**的文件，光加进 `retired_files.json` 是不够的：

- 更新器**只覆盖、不删除**，老用户目录里那份旧文件会一直留着；
- MXU 是把整个 `resource` 目录交给框架的（`interface.json` 的 `import` 只决定**界面上显示哪些任务**），
  所以旧文件**照样参与加载**；
- 框架对**跨文件重名节点**直接判定失败（框架日志 `key already exists`）→ 整个资源加载不成功，
  所有任务都用不了。

所以这类文件改为**发一个同名空壳过去**：内容只剩 `$` 开头的注释键（框架会跳过 → 定义 0 个节点），
更新时把它覆盖到用户目录，重名当场消失，**任何一次启动都不会失败**。

v26.09.11 就是这么处理这 5 个文件的：

| 已下架的旧文件 | 节点去哪了 |
|---|---|
| `resource/pipeline/Dailyrewards.json`、`EventReward.json` | 合并进 `RewardRoutine.json` |
| `resource/pipeline/Operate.json`、`Sociaty.json`、`Square.json` | 合并进 `DailyRoutine.json` |

对应的 5 个任务文件（`tasks/Dailyrewards.json` 等）不参与管线加载，直接进了 `retired_files.json`
由启动器删除。用户侧**不需要任何操作**，程序自己处理。

