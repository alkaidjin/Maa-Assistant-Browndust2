# Debug 文件和截图
debug/
debug/*.png
debug/on_error/*.png
*.jpg
*.jpeg

# 日志文件
*.log
go-service.log
maa*.log
mxu-*.log

# 大文件和编译产物
*.pdb
*.exe
*.dll
*.cache
cache/

# 系统文件
.DS_Store
Thumbs.db

# 用户配置（MXU / MaaPiCli 首次启动会自动生成；整个目录不入仓，
# 否则会冲掉每个用户的真实实例 —— maa_option.json / maa_pi_config.json 曾漏网）
config/

# 大模型文件（如果用 Git LFS 或单独下载）
resource/model/ocr/

# 运行时文件
*.log
*.pdb
*.tmp

# Python 字节码缓存
__pycache__/
*.pyc

# 更新器运行时产物
updater_cache.json
updates/

# Agent 工作区记忆（不属于软件本身）
.workbuddy/

# 防御性忽略：这个 0 字节文件由旧版 launcher.bat 注释里的 ">" 重定向产生（不是 MXU 的编码 bug）。
# v26.09.7 起 launcher.bat 已改为纯 ASCII 且不含 ">"，源头已消除；规则保留，以防老目录里的残留被误提交。
鑚姩

# local, machine-specific files
*.lnk

# 维护者自用工具（agent 辅助脚本、图标工具、打包器 / 备份器的双击入口）
# 2026-09-20 起 **改为入库**：这些文件此前既不在仓库、也不在用户包，属于「本地一丢就
# 彻底找不回」的真空区。它们含本机路径，只对作者这台机器有意义，但正因如此更需要版本
# 备份 —— 仓库是 public，介意暴露本机路径时请自行取舍。
# 注意：「进不进仓库」与「进不进用户包」是两套独立机制。入库后它们仍**不会**进发布包，
# 由 tools/build_release_zip.py 的 EXCLUDE_FILES 负责排除。
# 备份策略见 tools/backup_maintainer.ps1（双击「备份维护版.bat」）。
